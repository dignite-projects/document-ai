{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "extract",
  "projectType": "library",
  "sourceRoot": "packages/extract/src",
  "prefix": "lib",
  "tags": ["scope:lib", "type:feature-shell"],
  "targets": {
    "build": {
      "executor": "@angular/build:ng-packagr",
      "outputs": ["{workspaceRoot}/dist/extract"],
      "options": {
        "project": "packages/extract/ng-package.json"
      },
      "configurations": {
        "production": {
          "tsConfig": "packages/extract/tsconfig.lib.prod.json"
        },
        "development": {
          "tsConfig": "packages/extract/tsconfig.lib.json"
        }
      },
      "defaultConfiguration": "production"
    },
    "test": {
      "executor": "@angular/build:unit-test",
      "options": {
        "tsConfig": "packages/extract/tsconfig.spec.json",
        "buildTarget": "extract:build",
        "runner": "vitest"
      }
    },
    "lint": {
      "executor": "@angular-eslint/builder:lint",
      "options": {
        "lintFilePatterns": [
          "packages/extract/**/*.ts",
          "packages/extract/**/*.html"
        ]
      }
    }
  }
}
