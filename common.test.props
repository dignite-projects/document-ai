<Project>
    <PropertyGroup>
        <LangVersion>latest</LangVersion>
        <!-- MAAI001: Microsoft.Agents.AI experimental APIs (AgentSkill / AgentClassSkill etc.).
             Suppressed in common.props for src; mirror it here so test projects compiling
             against the same surface stay clean. -->
        <NoWarn>$(NoWarn);CS1591;CS0436;MAAI001</NoWarn>
        <NuGetAudit>false</NuGetAudit>
        <GenerateRuntimeConfigurationFiles>true</GenerateRuntimeConfigurationFiles>
        <GenerateAssemblyConfigurationAttribute>false</GenerateAssemblyConfigurationAttribute>
        <GenerateAssemblyCompanyAttribute>false</GenerateAssemblyCompanyAttribute>
        <GenerateAssemblyProductAttribute>false</GenerateAssemblyProductAttribute>
    </PropertyGroup>

    <Target Name="NoWarnOnRazorViewImportedTypeConflicts" BeforeTargets="RazorCoreCompile">
        <PropertyGroup>
            <NoWarn>$(NoWarn);0436</NoWarn>
        </PropertyGroup>
    </Target>

    <ItemGroup>
        <None Remove="**\*.abppkg"/>
        <None Remove="**\*.abppkg.analyze.json"/>
        <Content Remove="$(UserProfile)\.nuget\packages\*\*\contentFiles\any\*\*.abppkg*"/>
    </ItemGroup>

</Project>
